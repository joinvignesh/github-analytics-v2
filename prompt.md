activity for 5-3-2026

1. blend issues and comments - build a gantt chart vizz
2. implemnent comman filter across different grain - use date range filters.


ativity for 6-3-2026

identify and listout the nodes, the nodes can be
    input / output of data from a system [json data ingestion into cloud storage, data into snow RAW schema, data into all dbt schema, data away from mart sche ma ]
    list of all the cloud scheaduler [throughout the pipeline] with their scheaduled time - log - status
    list of all the cloud run [throughout the pipeline] - last updated - last run status
    

1. setup a detailed log summary at each node.
contents of the log summary as follows:
    execution start timestamp at each node
    summarize the status of execution [pass / fail / critical] at each node
    summarize the number of rows processed at each node
    identify critical data-points and summarise the null / blank identified at each node
    execution end timestamp at each node













